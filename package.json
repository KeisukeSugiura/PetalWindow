{
  "name": "PetalWindow",
  "version": "1.0.0",
  "description": "A sharing window images system with web browser",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start ": "./node_modules/.bin/electron ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.6.2"
  }
}
